import SwiftUI

struct HomeView: View {
    var body: some View {
        NavigationStack {
            Text("LabFlow")
                .navigationTitle("LabFlow")
        }
    }
}

#Preview {
    HomeView()
}
